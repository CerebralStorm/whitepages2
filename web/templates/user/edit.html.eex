<h2>Edit user</h2>

<%= render "form.html", changeset: @changeset,
                        action: organization_user_path(@conn, :update, @user, @organization_id) %>

<%= link "Back", to: organization_user_path(@conn, :index, @organization_id) %>
