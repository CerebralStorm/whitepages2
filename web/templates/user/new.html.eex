<h2>New user</h2>

<%= render "form.html", changeset: @changeset,
                        action: organization_user_path(@conn, :create, @organization_id) %>

<%= link "Back", to: organization_user_path(@conn, :index, @organization_id) %>
